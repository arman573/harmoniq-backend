# AI Arman fast resolver live trigger — post WIF fix

The Workload Identity Provider now explicitly allows `arman573/harmoniq-backend` on `refs/heads/ops/ai-arman-resolver-candidate-20260820`.

Run the guarded fast resolver deploy. Do not send customer messages or perform supported writes. Verify the zero-traffic candidate first, including real read-only prepare, latency, output wrapper contract, explicit-approval denial, unchanged positive production traffic, and only then retag the proven revision onto the existing stable resolver URL.
